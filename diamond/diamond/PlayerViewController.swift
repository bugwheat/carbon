//
//  PlayerviewController.swift
//  diamond
//
//  Created by Daniel Ostashev on 05/11/2022.
//

import UIKit

class PlayerViewController: UIViewController {

    @IBOutlet var playButton: UIButton!

    required init?(coder: NSCoder) {
        super.init(coder: coder)
    }

    override func viewDidLoad() {

//        playButton.setImage(UIImage(systemName: "play"), for: .normal)
//        playButton.imageIn
//        let playIcon = UIImageView(image: UIImage(systemName: "play"))
//        playButton.addSubview(playIcon)
//        NSLayoutConstraint.activate([
//            playIcon.leftAnchor.constraint(equalTo: playButton.leftAnchor),
//            playIcon.rightAnchor.constraint(equalTo: playButton.rightAnchor),
//            playIcon.topAnchor.constraint(equalTo: playButton.topAnchor),
//            playIcon.bottomAnchor.constraint(equalTo: playButton.bottomAnchor),
//        ])
    }

}
